import 'package:flutter/material.dart';

class WordLoadingWidget extends StatelessWidget {
  const WordLoadingWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/search.gif');
  }
}
